day 3

ALTER TABLE Client ADD PRIMARY KEY (CNO);

ALTER TABLE Client DROP PRIMARY KEY;

ALTER TABLE Employee ADD CONSTRAINT FK_EMP FOREIGN KEY (Manager) REFERENCES Employee(EmpID);

ALTER TABLE Employee DROP CONSTRAINT FK_EMP;

ALTER TABLE Employee ADD CONSTRAINT FK_DEPT FOREIGN KEY (DeptNo) REFERENCES Department(DeptNo);

ALTER TABLE Employee ADD UNIQUE (Job);

ALTER TABLE Employee ADD CHECK (Comm > 400);

ALTER TABLE Employee MODIFY Salary DEFAULT 25000;

ALTER TABLE Employee DROP UNIQUE(Job);

SELECT * FROM USER_CONSTRAINTS WHERE table_name = 'EMPLOYEE';